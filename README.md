# return-scenario-engine

Portable, dependency-free historical return scenarios from caller-owned normalized index series. The initial API is shared by TypeScript (Node.js 20+) and Python (3.11+), with synthetic cross-language conformance fixtures.

The package accepts only explicit artifact fields. It does not fetch prices, select assets, retain provider metadata, or implement product routes and UI.
