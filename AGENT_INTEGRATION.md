# Integrating return-scenario-engine

Install the matching public artifact and map explicit normalized-index inputs to `calculateScenario`. Keep asset selection, provider data, routes, and presentation local. Compare legacy and package outputs on identical inputs. A first migration rollback restores the consumer commit, adapter, dependency, and lockfile together. Use synthetic data in public diagnostics.
